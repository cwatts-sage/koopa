
## 🚨 NFL Pool: WFP_TEST_MODE still TRUE in production (found 2026-08-29)

- **Verified live:** `az staticwebapp appsettings list --name watts-football-pool --resource-group NFL`
  → `WFP_TEST_MODE = true`
- **Resource name is `watts-football-pool` (RG `NFL`)** — NOT the hostname
  `zealous-glacier-0155d740f`. Using the hostname makes az say "not found".
- **Impact:** `api/picks/index.js:78`
  `const gameLocked = TEST_MODE ? scored : (scored || (lockTime && now >= lockTime));`
  In test mode a game only locks once **scored**, so picks remain editable AFTER kickoff
  until the game is scored. Pool integrity hole for real play.
- **Season is LIVE:** 2026 matchups loaded w/ real kickoffs + broadcasts + lockTimes.
  Week 1 opener = Patriots @ Seahawks, Wed 2026-09-09 8:20 PM ET.
- **Fix when Chris approves:**
  `az staticwebapp appsettings set --name watts-football-pool --resource-group NFL --setting-names WFP_TEST_MODE=false`
  (then confirm `locked` starts honoring lockTime ~1hr before kickoff)
- **Status: WAITING ON CHRIS** — production config change, needs his approval.

## Notes
- PLAN.md is stale: says "no active cron jobs yet", but NFL Schedule Refresh cron
  (b576c185-223f-4828-90da-563ba7fbb84d, Tuesdays 6am ET) is live and enabled.
- The old "weekly offseason update" cron referenced in MEMORY.md no longer exists.

## 🚨 SECOND FINDING (2026-08-29 evening): users table is EMPTY

- **Verified via Azure Table Storage directly** (storage account `wattsfootballpool`, RG `NFL`):
  - `users`     → **0 rows**  ← all registered accounts GONE
  - `standings` → 0 rows
  - `picks`     → 0 rows
  - `matchups`  → 271 rows (2026 season, healthy)
- MEMORY.md says Chris, Stephanie, Geo, and Marion Box were all registered. None remain.
- **NOT caused by the season loader.** `load-2026-season.js` only deletes matchups + picks,
  and `resetStandings()` is a non-destructive upsert that preserves names. Users were
  wiped by something else (manual clear or table recreate) — unknown, predates this check.
- **Not recoverable:** storage is Standard_LRS, blob soft-delete disabled, and Azure Table
  Storage has no point-in-time restore here. → **Everyone must re-register before Week 1.**
- Auth detail: users live at PartitionKey `"user"`, RowKey = normalized email
  (`api/register/index.js`, `api/login/index.js`).

### Pool readiness for Week 1 (opener Wed 2026-09-09, 8:20 PM ET — Patriots @ Seahawks)
| Item | State |
|---|---|
| 2026 schedule loaded | ✅ 16 games/wk, wks 1-18, no midnight placeholders |
| Old test data cleared | ✅ picks/standings empty, season stamped 2026-2027 |
| Wk 17/18 broadcasts | ⚠️ blank — NORMAL (NFL flexes; Tuesday refresh cron fills in) |
| WFP_TEST_MODE | 🚨 still `true` — pick locking disabled (needs Chris) |
| Registered users | 🚨 ZERO — everyone must re-register (needs Chris) |

## ✅ Preflight test of registration flow (2026-08-29 evening)

Ran an END-TO-END test with a throwaway account, then fully cleaned it up.

- **Empty-users finding CONFIRMED, not a false alarm.** `api/shared/storage.js` uses table
  name `users` and the same `AZURE_STORAGE_CONNECTION_STRING` app setting I queried. Names match.
- **The app itself is healthy:**
  - `POST /api/register` → HTTP 201 + JWT ✅
  - `POST /api/login` → HTTP 200 + JWT ✅
  - `POST /api/picks` (header `X-Wfp-Token`) → HTTP 200, pick stored ✅
- So the ONLY thing standing between the Troop and Week 1 is re-registration.
  Nothing is broken; the accounts are simply gone.
- **Cleanup done** — test account removed from `users`, `picks` (pk `week-01`),
  and `standings` (pk `2026`). All three tables verified back to 0 rows.
- ⚠️ **Gotcha learned:** submitting a pick AUTO-CREATES a standings row. Any future
  prod testing must clean up all THREE tables, not just users.
- Also confirmed `"lockedGames":[]` on a Week 1 submit — consistent with WFP_TEST_MODE=true.
