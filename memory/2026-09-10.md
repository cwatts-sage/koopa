# 2026-09-10 (Thursday)

## Chris's overnight direction (03:33 / 03:37Z)
- *"Regular season standings updated to 1 point for each player. That should only be
  counting the weekly points award only."*
- *"Season ranks on F1. Make the change to implement the scoring we agreed on"*

→ Season standings must rank on **F1 weekly awards only**, NOT raw correct picks.

## ✅ Code change made and pushed (commit 8da8fa7)
`api/standings/index.js` now derives `totalPoints` from `weeklyF1` (integer tenths);
raw correct picks retained separately as `correctPicks`. `score-week.js` only awards the
F1 grid once a week is fully complete. Syntax checked; logic verified locally against
current storage values → everyone reads **0.0 pts mid-week** with correct picks tracked
alongside, exactly as Chris asked.

## 🚨 BLOCKER: the change is NOT LIVE — GitHub Actions deploy is not running

- Live API still returns `totalPoints=1, f1=None, correct=None` → **old deployed code**.
- `az staticwebapp environment list` → **lastUpdated 2026-09-08T19:02Z**. My push at
  ~05:30Z today produced **no new deployment**.
- Workflow `.github/workflows/azure-static-web-apps.yml` exists and is committed, and
  triggers on push to `main` — but it needs repo secret
  **`AZURE_STATIC_WEB_APPS_API_TOKEN`** (line 33).
- **`gh` CLI is NOT installed**, so I cannot inspect Actions runs or repo secrets.

**Most likely cause:** the `AZURE_STATIC_WEB_APPS_API_TOKEN` secret was never added to
the GitHub repo (the workflow file itself was only created 2026-09-08 11:58 local and
`.github/` was untracked until my commit today — so it may never have run successfully).

**Deployment token is on record** in MEMORY.md if Chris needs to set the secret.

**Needs Chris:** add the repo secret (or authorise installing `gh` so I can diagnose the
Actions run properly). Until then the F1 standings fix sits correct-but-undeployed.

⚠️ Also answers Chris's earlier question *"I assume we are using GitHub Actions to build
the app now"* — the workflow EXISTS but has never successfully deployed.

## ✅ RESOLVED: deploy went through, F1 standings are LIVE

Earlier this morning I reported the fix as "pushed but not deployed" and blamed a missing
`AZURE_STATIC_WEB_APPS_API_TOKEN` secret. **That resolved itself** —
`lastUpdatedOn = 2026-09-10T09:40:03Z`, so GitHub Actions DID deploy (the run just hadn't
completed when I checked at ~05:25Z; I called it a blocker too early).

**Live API now returns exactly what Chris asked for:**
```
1. Conner   total=0  f1=0  correct=1
...
11. Casi    total=0  f1=0  correct=0
```
- Season ranks on **F1 points only** → all 0.0 mid-week (grid not awarded until the week
  closes Monday) ✅
- Raw correct picks retained separately as `correctPicks` ✅

**So GitHub Actions IS working now** — answers Chris's question definitively. Pushing to
`main` deploys.

⚠️ **Lesson (4th time):** I declared a blocker ~5 minutes after pushing, before the CI run
could finish. Same pattern as the users-wipe, "Chris is unreachable", and "site is down" —
concluding from a snapshot instead of allowing for propagation/async completion.
**Rule: for anything asynchronous (CI, DNS, deploys), re-check on a delay before escalating.**

## Game Center mockup (parallel session)
A parallel session built `projects/nfl-agent/mockups/game-center-mock.html` from real
NE@SEA data (19 drives, 179 plays), hit iMessage's attachment limit on the 80KB HTML, and
resent as PNGs. Chris asked to see a mockup; that's delivered. Nothing deployed.

## ✅ Game Center: direction-of-travel question ANSWERED (was the mockup's biggest unknown)

The mockup shipped with a caveat: *"Direction of travel is my best guess... teams switch
ends at quarters, and ESPN's yardLine is absolute."* **Tested it against real data.**

Used 2025 Wk1 DAL@PHI (event 401772510), all 16 drives:
```
DAL: ascending 0, descending 8  -> CONSISTENT
PHI: ascending 8, descending 0  -> CONSISTENT
```
Sample: `Q1 DAL 53->0`, `Q1 PHI 30->100`, `Q2 DAL 69->23`, `Q2 PHI 19->100`.

**Conclusion: `yardLine` is absolute on a fixed 0-100 axis and each team's direction of
travel is CONSTANT for the whole game — it does NOT flip between quarters.** ESPN
normalises this. So the mockup can simply pin one team to ascending and the other to
descending for all 4 quarters. The feared Q2/Q3 bug does not exist.

## ⚠️ NEW FINDING: drives data for last night's game is MISSING
- 2026 Wk1 NE@SEA (event 401872657): `drives` key **absent** from `/summary`, and
  `sports.core.api` drives endpoint returns **count 0**.
- 2025 Wk1 DAL@PHI (event 401772510): `drives.previous` = **16** drives, fine.

So it is NOT a general "ESPN purges old drives" problem — older games retain them. Last
night's game specifically has no drive data right now (still settling, or a gap on ESPN's
side). The mockup was built from 19 drives hours earlier, so the data WAS there and has
since gone.

**Implication for the live feature Chris wants:** drive data availability is not
guaranteed even for a completed game. Any Game Center feature needs to handle
`drives` being absent, and ideally cache drives once fetched rather than re-fetching.

## 🔴 CONFIRMED: ESPN drive data for the 2026 opener is GONE (not a lag)

Rechecked 2h later — still absent. Broadened the test:

| game | drives |
|---|---|
| 2026 Wk1 NE@SEA (401872657) | **0** |
| 2025 Wk1 DAL@PHI (401772510) | 16 |
| 2024 Wk1 (401671789) | 20 |

Also `sports.core.api .../plays` → **count 0** for the 2026 game. So both drives AND plays
are unavailable for last night's game via every endpoint tried.

**Proof the data existed:** `projects/nfl-agent/mockups/game-data.json` (68KB, written
02:49 today) contains **19 cached drives** from that exact game. It was fetched, then
withdrawn from ESPN's API within hours.

**Why this matters for the Game Center feature Chris wants:**
1. **Caching is mandatory, not optional.** Fetch-on-demand would show a rich game page
   right after kickoff and an empty one the next morning.
2. Older seasons retain drives fine, so this looks like a gap/delay in ESPN's 2026
   pipeline rather than a deliberate purge — but we cannot rely on it.
3. The feature should persist drives/plays into our own storage as games progress
   (the existing Tue/Thu cron could do this), then serve from our copy.

**Tonight's 49ers @ Rams is the live test** — worth sampling `drives` DURING the game to
confirm it populates live, and immediately after to see whether it survives.

## 🏈 Geo's "Week Tally" Results proposal (relayed by Chris ~15:30 ET)

Geo proposed the Results tab show each week ranked descending with position,
F1 points, and record — e.g. `1st Chris 25 12/16 / 2nd Steph 16 11/16 / 2nd Ralph 16 ...`,
locked once the last game ends, with Standings unchanged. Asked whether that
makes more sense for the Results column, and pointed out how ties shift position.

**Verdict: he's ~90% describing what's ALREADY built — the gap is display only.**

- `scripts/score-week.js` already has the full F1 grid `[25,18,15,12,10,8,6,4,2,1]`
  (stored as integer TENTHS `F1_POINTS_X10`) + `awardF1PointsX10()` with
  standard-competition-ranking tie splits. Geo's tie intuition matches the code.
- `api/standings/index.js` already ranks the season on F1 points (`scoring: "f1"`),
  correct picks as tiebreaker. Geo's "no change to Standings" = already true.
- **The actual gap:** `api/weekResults/index.js` returns only `{name, picks, correct, total}`
  — no F1 points, no position. Frontend `renderResults()` (app.js ~line 792) renders
  "Week Scores" as medals 🥇🥈🥉 by array index, so **tied players show different
  medals** and no points column. That's the real bug Geo is sensing.
- Fix scope: add f1/position to weekResults response (reuse awardF1PointsX10),
  render position + points; only award/lock when every game has a winner.

**Verified Geo's exact scenario against the REAL scoring fn (week=1):**
```
1  Chris 25    12/16      5  Casi/Kelly/Geo 8 each  9/16
2  Steph 16.5  11/16      8  Conn/Merc 3 each       8/16
2  Ralph 16.5  11/16      10 Mari 1                 7/16
4  Sean  12    10/16      11 Don  0                 6/16
```
Pot = 101.0 = full grid, exactly conserved. Geo said 16 for the 2nd-place tie;
code pays **16.5** (P2+P3 = 18+15 = 33 split two ways). Geo also had 6 for the
8th tie; code pays **3** (P8+P9 = 4+2 = 6 split two ways). His method was
"split the pot" — right idea, arithmetic slightly off. Code is correct.

## ⚠️ PROD OUTAGE FOUND while checking: site returns 404
- `https://thankful-grass-00148f40f.6.azurestaticapps.net/` → **404 at ROOT**,
  and all of /api/standings, /api/week-results, /api/live, /api/matchups → 404.
- Not auth, not the API routes (function.json route `week-results` is correct).
- DNS resolves fine (trafficmanager → 52.167.15.22), host answers, so it's
  app-level: frontend content appears unpublished / deployment gone.
- Same with browser UA. **Needs investigation — flagged to Chris.**

## Deploy path — evidence on whether GitHub Actions works

Chris approved: *"Yes, fix memory and lets fix the display as suggested."* Memory fix was
done by a parallel session. Display fix is committed as `aa8ec7b` but **was not live**.

**Key evidence that Actions DOES work:**
- `67645b0` pushed 09:31Z → SWA `lastUpdatedOn` **09:40Z**. Nine-minute lag, successful
  deploy. That is how my F1 standings fix went live.
- So the `AZURE_STATIC_WEB_APPS_API_TOKEN` secret **is** configured and the workflow runs.
  The earlier "missing secret" theory was wrong.

**But `aa8ec7b` (pushed 19:43Z) had not deployed 2h13m later** — far beyond the 9-minute
norm, so this is a genuine miss, not impatience on my part (deliberately checked, given
I called a blocker too early this morning).

**Action taken:** pushed empty commit `a268dd1` at 21:57Z to re-trigger the workflow.
Non-destructive, no code change. Monitoring.

**Recommendation for Chris's question #1 (deploy path):** do NOT do a local `swa deploy`.
Actions is proven working; local macOS deploys are what caused the 600-perms EACCES
outage the workflow was written to avoid. If a push occasionally fails to trigger, an
empty re-trigger commit is the safe remedy.

## ❗ CORRECTION: GitHub Actions is NOT deploying — SWA is not linked to the repo

I concluded "Actions works" from the 09:31Z push → 09:40Z deploy correlation. **Wrong.**

`az staticwebapp show` returns:
```
provider: "SwaCli"   repo: null   branch: null
```

The Static Web App has **no GitHub repository linked**. So GitHub Actions cannot be
deploying it — the 09:40Z deploy must have come from a manual `swa deploy` run by a
parallel session, and the 9-minute gap was coincidence.

Evidence this is right:
- `aa8ec7b` (19:43Z) — no deploy after 2h40m
- empty re-trigger `a268dd1` (21:57Z) — no deploy after 29 min
- `lastUpdatedOn` frozen at 09:40:03Z throughout

**So Chris's question #1 is genuinely open and needs his decision:**
- **(a) Link the repo to SWA / set up Actions properly** — requires connecting GitHub to
  the Static Web App (changes deployment setup, and `provider` would move off `SwaCli`).
- **(b) Manual `swa deploy`** — works today (it's what shipped 09:40Z) but carries the
  known macOS 600-perms EACCES risk the workflow was written to avoid. Note the workflow's
  `chmod` normalisation step could be replicated locally before deploying to mitigate it.

**Lesson (5th time, same shape):** I inferred causation from a timing correlation instead
of checking the authoritative config. `az staticwebapp show` would have told me in one
call. **Rule: verify the mechanism, not just the correlation.**

## ✅ Second lock fired correctly (23:56Z check)
- Patriots @ Seahawks — 🔒 LOCKED (Wed)
- **49ers @ Rams — 🔒 LOCKED** (locked 23:35Z / 7:35 PM ET as designed)
- Bucs @ Bengals (Sun) — open, locks 1hr before its own kickoff

Per-game locking confirmed working two nights running. No whole-week lock, no early lock.

## ❗❗ CORRECTION: drive data was NEVER missing — I queried the WRONG EVENT ID

Earlier today I reported "2026 Wk1 NE@SEA drives are GONE from ESPN" and built a whole
recommendation (mandatory caching) on it. **That finding was false.**

The event IDs are:
- **401872656** = New England @ Seattle (Wed, Final)
- **401872657** = San Francisco @ LA Rams (Thu, tonight)

I had been querying **401872657** believing it was the NE@SEA game. It returned 0 drives
simply because *that game had not been played yet*. Verified now:

| event | game | drives |
|---|---|---|
| 401872656 | NE @ SEA (Final) | **19 previous** ✅ |
| 401872657 | SF @ LAR (live, Q1) | **3 previous + current** ✅ |

19 drives matches exactly what `mockups/game-data.json` cached. Nothing was ever withdrawn.

**Two genuinely useful facts survive, and one is now proven:**
1. ✅ **Drives DO populate live mid-game** — `previous: 3, current: true` during Q1. This
   was the real open question for Chris's Game Center feature. **It works live.**
2. ✅ Direction-of-travel finding (constant per team, no quarter flip) still stands — that
   used a different, correctly-identified 2025 event.

**Retracted:** "caching is mandatory because ESPN withdraws data." Caching may still be
sensible for rate-limit/performance reasons, but NOT for the reason I gave.

**Lesson (6th correction this week, and the most avoidable):** I never verified that the
event ID matched the game name. One line — printing `event['name']` alongside the id —
exposed it instantly. **Rule: when an API result contradicts known-good prior data
(the mockup had 19 drives), suspect my query before concluding the source changed.**

## Game 2 — SF @ LAR (Thu night)
- Q3 status: **49ers 17, Rams 7**
- Pool split: **Rams 8** (Conner, Donna, Ralph, George, Kelly, Marion, Sean, Stephanie)
  vs **49ers 3** (Chris, Mercedez, Casi)
- If SF holds: Chris/Mercedez/Casi gain on the field. Casi (0/1 so far after the lone
  Patriots pick) would get back to even-ish; the 8 Rams backers drop a game.

## ✅ Live-drive behaviour confirmed over time
- Q1 check: previous 3, current=True
- Q3 check: previous 13, current=True
Drives accumulate steadily through a live game. Game Center as a LIVE feature is viable.
