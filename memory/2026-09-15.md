
## NFL Pool — Week 1 FINAL (Tue 2026-09-15, 12:30 AM ET cron)
- Ran `run-scoring.sh` → Week 1, 16/16 games matched, all final after MNF.
- **Week 1 winner: Marion Box, 13/16, 25 pts** 🏆
- Podium: Marion Box 25 · George 16.5 · Sean Neely 16.5 (tie P2)
- Chris finished P4 (11/16, 10 pts), tied w/ Ralph + Mercedez.
- 11 players, 0 without picks. Week pot: 101 pts.
- Season standings = week standings (first week of season).
- Reported to Chris via imsg send-rich → delivery verified via `imsg history --chat-id 2`.
- Note: `message` tool not in this cron run's policy-filtered toolset; CLI fallback `send-rich` worked as documented in TOOLS.md.
# 2026-09-15 (Tuesday) — Week 2 begins

## Daily checks: all green
imsg IMCore v2, gateway 2026.7.1-2.

## ✅ Scoring is now AUTOMATED — three crons in place
1. `b576c185` NFL Schedule Refresh — Tue 6:00 AM
2. `17c934a9` Score Wed/Thu games — Thu+Fri 12:30 AM
3. `24011e8c` Score Sunday slate + MNF (week close) — Mon+Tue 12:30 AM (ran 5h ago, ok)

So Week 2 will score itself; no manual `score-week.js` runs needed unless something breaks.

## ✅ Verified Tuesday refresh cron won't disturb scored Week 1
Dry-ran weeks 1-2: **Week 1 = 16 "Skipped (final)"**, Week 2 = 16 already correct,
0 updates, 0 unmatched. `Picks UNTOUCHED / Standings UNTOUCHED`. Safe.

## Week 1 summary delivered
Chris asked for a group-postable summary (03:19Z); a parallel session produced the full
standings + analysis writeup. Note it correctly flagged that the football iMessage group
is still dormant per Chris's Feb decision, so Chris posts it himself.

## ✅ DEPLOY UNBLOCKED — Results display is LIVE (19:58Z)

Five days after `aa8ec7b` was committed, the deploy finally shipped via a manual
`swa deploy` (parallel session). `lastUpdatedOn` moved 2026-09-10T09:40Z → **2026-09-15T19:58Z**.

**Verified live on `/api/week-results?week=1`:**
```
scored: True  complete: True  games: 16/16
1st  Marion Box   f1=25    correct=13
2nd  George       f1=16.5  correct=12
2nd  Sean Neely   f1=16.5  correct=12
4th  Chris        f1=10    correct=11
4th  Ralph        f1=10    correct=11
```
**Position labels are correct standard-competition ranking** — 1st / 2nd / 2nd / **4th**.
Tied players share a place and the next drops below all consumed slots. This fixes the
old bug where tied players were handed different medals by array index.
`week-score-pos` also confirmed present in the deployed `app.js`.

## CI/CD disagreement — Chris was right, and the correction was made properly
Chris: *"the SP credentials is still a user and a secret that can both be used when the
runner runs to use azure cli to authenticate to azure and then deploy."*

A parallel session had claimed the SP was "the wrong credential," then correctly conceded:
the SP **is** sufficient for the Azure half — it authenticated and pulled the deploy key
via `az staticwebapp secrets list`. A runner doing `azure/login@v2` → fetch key → deploy
works, and is better than a stored token (rotatable, RG-scoped). The only narrow truth is
that *something* must write the secret into GitHub once, and that single bootstrap write
needs a GitHub credential.

**Deploy gotcha recorded:** CLI deploy fails with "Function language info isn't provided"
despite `staticwebapp.config.json` declaring `apiRuntime: node:18` — it isn't read during
CLI deploy. Needs explicit `--api-language node --api-version 18`. **The GitHub workflow
will hit the same wall** (it also uses `skip_app_build: true`), so that flag pair must be
in the action config or the first green CI run still won't ship the API.

## Still open
- CI/CD not wired. Proposed: add `AZURE_CREDENTIALS` (SP JSON) as repo secret, rewrite
  workflow to `azure/login` → pull key via `az` → deploy. Needs Chris to paste the secret
  (or provide a PAT). **Chris's preference is the SP route.**
- Chris had a truncated message ("first update the...") — one requested change may never
  have arrived. Parallel session asked him to re-send.

## ✅ CI/CD workflow rewritten for Chris's SP approach (commit 3d621a9)

**Proved Chris's point first:** ran `az staticwebapp secrets list` as the SP —
it returned the 119-char deploy key. SP identity confirmed as
`type: servicePrincipal`, tenant `da8ff464...`. So the runner CAN do
`azure/login` → fetch key → deploy, exactly as he described.

**New workflow** (`.github/workflows/azure-static-web-apps.yml`):
- `azure/login@v2` with `AZURE_CREDENTIALS` (SP JSON) — the ONLY secret needed
- deploy key fetched at run time + `::add-mask::`, never stored as a second secret
- **`api_language: node` / `api_version: "18"` added** — fixes the
  "Function language info isn't provided" failure, since `apiRuntime` in
  staticwebapp.config.json is NOT read on the `skip_app_build: true` path.
  Without this the first green CI run would still not ship the API.
- chmod normalisation retained (api/shared/f1.js caught at 600 before)

**Remaining step is Chris's alone:** add `AZURE_CREDENTIALS` as a repo secret on
`cwatts-sage/NFL`. Nothing on this box can write a GitHub secret (no gh CLI, no PAT,
SSH deploy key can't touch settings). Once added, push-to-deploy works and the manual
`swa deploy` path can be retired.
